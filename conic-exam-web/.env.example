# Firebase web config is public configuration, not a service-account secret.
# Defaults target the existing isolated ConicTypst identity/control plane.
VITE_FIREBASE_REGION=asia-southeast1
VITE_APP_CHECK_SITE_KEY=
