{
  "name": "conictypst-platform-functions",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated ConicTypst identity and product-entitlement control plane.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:emulator": "node --test --test-concurrency=1 test/integration.emulator.test.js test/exam-integration.emulator.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.6.0"
  }
}
